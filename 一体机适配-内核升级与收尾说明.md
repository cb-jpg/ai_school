# 一体机（55 寸竖屏）适配收尾说明：内核升级 + 真机三轮修复

- **日期**：2026-09-18
- **分支**：`kiosk-chrome95-parity`（合并自 `a7c6d6e` 之上的后续修复）
- **目标设备**：KP-AIJH-550 竖屏一体机（RK3588 / Android 12 / 触摸 CSS 视口 960×1627）
- **前置文档**：`竖屏适配修改说明.md`（2026-09-15，竖屏大屏第三形态）、`一体机真机调试指南.md`（adb/CDP 真机连接方法）

---

## 〇、先回答最重要的问题：手机和一体机是同一套代码吗？

**是。一套代码、一个 APK 同时服务手机和一体机**，运行时自动识别：

- `frontend/src/renderer/src/utils/device-profile.ts` 判定：短边 ≥640 且带触摸（红外屏 `maxTouchPoints=0` 时按 Capacitor 原生/粗指针兜底）→ 一体机模式，给 `<html>` 加 `kiosk` 类
- 一体机模式专属行为：420×712 设计盒 + `transform: scale()` 整体缩放、dpr 钉死、`--app-vh` 视口守卫、Chakra 断点改写为 9991px+（永远命中手机档）
- 手机/桌面浏览器不走以上任何分支，行为与历史版本一致
- 浏览器调试可在 URL 加 `?kiosk=1` 强制进入一体机模式（无需真机）

---

## 一、根因：不是 CSS 写错，是 WebView 内核太老

真机症状（胶囊竖排、蓝色丢失、按钮原生灰框、专题页样式崩坏、登录页白屏）逐项排查后指向同一个根因：**机器出厂 WebView 是 Chrome 95（com.android.webview 95.0.4638.74），不解析 Chakra v3 依赖的 `@layer`（Chrome 99+ 才支持），整段组件样式被静默丢弃**。构建产物本身无问题（CSS 里 `@layer` 齐全）。

**修复 = 给机器换新内核（设备侧一次性操作，不在代码里）**：

```
adb install <Android System WebView 153 APK>          # com.google.android.webview，arm64
adb shell cmd webviewupdate set-webview-implementation com.google.android.webview
adb shell dumpsys webviewupdate                        # 确认 Current = com.google.android.webview 153.x
```

内核 95→153 后：横排胶囊、激活蓝、专题页排版、下半屏裁切（内容重新按设计稿排布后自然收回视口内）**全部自动恢复**。此前针对 Chrome 95 的两层 CSS 兜底，一层已删除（见下），登录页一层保留（`vw` 取物理视口的问题与内核无关）。

> ⚠️ 换机/恢复出厂后需重做此步。WebView APK 用 `com.google.android.webview` 包名（Google 官方通用版），从 apkcombo 等镜像站下载 arm64 变体即可。

---

## 二、本轮代码修复（3 处）

### 1. 删除 Chrome 95 按钮兜底 CSS（新内核下它反过来制造 bug）

`index.css` 中曾为 Chrome 95 加过：

```css
html.kiosk button:not(.animated-button) { background-color: transparent; ... }
```

Chrome 95 下它救了"按钮带原生灰框"；但内核升到 153 后，**无层 CSS 永远压过 `@layer` 里的 Chakra 样式**，所有 kiosk 按钮的底色被它清成透明——表现为：专题选项行里**被点击选中的那个选项蓝底消失、白字打在白底上**（用户报告的"被白色组件遮挡"，实际是按钮自己隐形），首页"开始对话"按钮同样中招。整块删除即可，手机端本来就不受影响。

### 2. 拖动人物命中点偏移 2.29 倍（"点人本体抓不住，点右上角才能拖"）

`use-live2d-model.ts` 共 5 处命中/拖拽换算：

```ts
// 旧：clientWidth 是 transform 缩放前的布局宽（420），指针坐标却是缩放后的物理像素（960 系）
const scale = canvas.width / canvas.clientWidth;   // = 2.29，命中点被放大抛向右下
// 新：rect.width 是缩放后的真实渲染宽，三端（手机/桌面/一体机）一致
const scale = canvas.width / rect.width;
```

拖动跟手性同理修复（拖动增量走同一换算）。

### 3. 移除左下角绿色诊断悬浮层

`components/canvas/kiosk-diagnostics.tsx` 整个删除（真机排障期用于拍照读视口/守卫值/内核版本，已完成使命；两处挂载与 import 一并清理）。需要时可从 git 历史找回。

---

## 三、交接须知（后续维护者看这里）

### 构建 APK（手机/一体机同一个包）

```
cd frontend
npm run build:web            # vite 构建 → dist/web（需 .env.web.local，见下）
npx cap sync android
cd android && ./gradlew assembleDebug   # 产物 app/build/outputs/apk/debug/app-debug.apk
```

- `frontend/.env.web.local`（gitignored）需含 `VITE_ACCESS_TOKEN=<网关访问令牌>`：一体机所在网络经网关访问后端，所有 `/api` 需带 `?token=`，缺它则 kiosk 端全部 401 掉回登录页。令牌找学校方/后台管理员（v2.2 正式包里也烤着同一个值）
- 正式发版走服务端 `local/pub_release.py` 流程（详见项目档案），版本号记得 bump versionCode

### 一体机登录机制

- localStorage：`kb_token`（JWT，含 exp，会过期）+ `kb_user`；过期后 401 → 自动清凭证回登录页，属正常现象，重新注入或用账号登录即可
- 调试注入方法见 `一体机真机调试指南.md`（CDP Runtime.evaluate 写 localStorage 后 reload）

### 真机调试

`一体机真机调试指南.md`：USB/无线 adb 连接、开发者选项开启、CDP 端口转发（`webview_devtools_remote_<pid>`）、截图/DOM 探查脚本用法。**调试完记得关 USB/无线调试**（展机公共区域）。
