# 数字人 App 并发压测报告（2026-09-06 凌晨）

**目标**：测出 App（`/client-ws` 数字人后端，183.36.243.124:12393）稳定运行支持的最大并发数。
**方法**：`scripts/concurrency_stress_test.py`（本地模拟 N 个 App 用户，各 1 条 WS 连接，每人 2 轮文本问答：
第 1 轮校情问题（触发 RAG），第 2 轮纯聊天；同级客户端 ~2s 内突发连入提问）。梯度 1→2→4→8→12→16→24→32→48→64→96，
逐级加压，失败自动复测一次；脚本内置四指标保险丝（MemAvailable<8G / load1>50 / swap>50% / IO PSI>50%，任一超标立即中止）。

**判稳标准**：连接成功率 100% + 每轮收到音频段与合成完成信号 + 开口中位 ≤ max(2.5×单人基线, 20s) + 整轮 p95 ≤ 90s + 服务器四指标无恶化。

## 结果总表

| 并发 N | 连接 | 轮成功 | RAG命中 | 开口 med/p95 (s) | 整轮 med/p95 (s) | 判定 |
|---:|---:|---:|---:|---:|---:|---|
| 1 | 1/1 | 2/2* | 1 | 5.0 / 7.0 | 8.1 / 8.4 | PASS（*首测两轮超时，30s 复测过，见"偶发"） |
| 2 | 2/2 | 4/4 | 2 | 2.9 / 6.9 | 3.3 / 7.0 | PASS |
| 4 | 4/4 | 8/8 | 4 | 2.9 / 10.8 | 3.5 / 17.8 | PASS |
| 8 | 8/8 | 16/16 | 8 | 3.3 / 8.7 | 7.7 / 12.9 | PASS |
| 12 | 12/12 | 24/24 | 12 | 4.5 / 10.0 | 6.6 / 16.3 | PASS |
| 16 | 16/16 | 32/32 | 16 | 6.0 / 11.1 | 8.2 / 14.8 | PASS |
| 24 | 24/24 | 48/48 | 24 | 12.5 / 22.4 | 15.0 / 27.0 | PASS |
| 32 | 32/32 | 64/64 | 32 | 11.2 / 17.1 | 13.0 / 20.3 | PASS |
| 48 | 48/48 | 96/96 | 48 | 16.8 / 28.4 | 19.8 / 33.3 | PASS |
| **64** | **64/64** | **128/128** | **64** | **24.0 / 38.1** | **27.0 / 42.4** | **PASS（可靠上限）** |
| 96 带RAG | **70-74/96** | 连上的全成 | 70-74 | 29.7-32 / 51.6-54.1 | 31.6-33.7 / 53.9-54.6 | **FAIL ×4 复现**（22-26 条握手失败） |
| 96 无RAG（对照） | 96/96 | 192/192 | 0 | 27.5 / 48.1 | 38.4 / 52.4 | PASS |

服务器全程无感：可用内存 44.2-46.9G（基线 ~46G）、load1 峰值 12（压测机本机基线 1）、IO PSI 恒 0%、
run_server RSS 1.49G→1.91G（约 +8MB/连接）、CPU 峰值 <1%（采样窗口）。压测后连接数回落 0，无残留。

## 结论

**稳定并发上限 = 64**（100% 成功）。**体验良好的实用区间 = 16 以内**。

| 区间 | 并发 | 开口中位 | 体验 |
|---|---|---|---|
| 流畅 | 1-8 | ~3s | 与单人几乎一致 |
| 良好 | 12-16 | 4.5-6s | 可接受等待 |
| 可用 | 24-32 | 11-12.5s | 明显但可忍 |
| 勉强 | 48 | ~17s | 演示勉强 |
| 极限 | 64 | ~24s | 100% 成功但等待久 |
| 失效 | 96（校情突发） | — | ~25% 用户连不上 |

## 瓶颈归因（有对照实验支撑）

1. **失效点不是服务器资源，是 RAG 同步检索阻塞事件循环**。`rag_service.py` 的 `_search_sync`（torch embedding）
   在 asyncio 事件循环内同步执行：96 人同时问校情问题 = 96 次串行 embedding，事件循环被卡数十秒，
   新 WS 握手排队饿死 → 客户端 15s 超时。对照组（同一 N=96，问题换成"讲个笑话"不过 RAG 门控）96/96 全通，4 次复现。
   裸连接探针（96 条仅握手即断，空载服务器）0.6s 全成，排除连接层与本地网络。
   **修法**：embedding 挪 `asyncio.to_thread`/executor，或检索排队合并；修后 96+ 可期。
2. **并发正确性缺陷（建议排期修）**：`basic_memory_agent.py:246` `self._memory` 是全连接共享单例状态——
   并发用户的对话互相污染上下文（A 问的会话 B 也能"记得"）；且任何人 `fetch-and-set-history` 都会改写共享记忆。
   低并发演示不易察觉，并发上来后是实际错误答案来源。
3. **上游偶发抖动**：凌晨 01:17 连单用户都碰到一次两轮 90s 超时（DeepSeek/edge-tts 偶发），30s 后重试即恢复。
   App 侧已有重发即恢复的能力；属于外部依赖固有抖动，非本服务瓶颈。

## 压测协议要点（复测/续测须知）

- 真实回复本版本**只通过 audio 消息下发**；`full-text` 只发 "Connection established"（连接问候）与 "Thinking..."（占位）。
  旧 `ws_token_e2e_test.py` 把 "Thinking..." 当回复文本，其 PASS 判据是弱的。
- `backend-synth-complete` 每轮发两次（`single_conversation.py:170` 与 `finalize_conversation_turn` 各一），
  收流逻辑必须去重，否则下一轮会吃到上一轮残留（首版压测脚本即踩此坑误判"轮 2 秒失败"）。
- 压测客户端不发 `audio-play-start`/心跳（App 协议有），对服务器是"只听不说"的最重等待情形；真实 App 的音频确认消息略增服务端消息量，不改变结论量级。
- 本测试为**文本问答链路**（RAG→LLM→TTS→音频下发）。语音提问每轮额外加一次本地 sherpa-onnx ASR（毫秒级 CPU），
  对"全班同时开口"的极端语音场景需另行专项压测。
- 明细数据：`scripts/concurrency_stress_results.json`（最后两次运行的分级明细+服务器指标时间线）；
  复测命令：`PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -u scripts/concurrency_stress_test.py --levels 32,64 --timeout 150 [--no-rag]`

## 对学校演示场景的建议

- 40 人班级：错峰提问（分 3-4 组）或限制同时提问 ≤16 人 → 体验良好。
- 大屏单点 + 学生围观场景（实际部署形态）无任何压力。
- 若要支撑百人并发：优先做上面第 1 条（RAG 异步化）+ 第 2 条（会话记忆按连接隔离）。

---

# 复测：扩容改造后 200 并发达成（2026-09-06 晚）

**改造内容**（commit `89d6bd8` + `25a9f2a`，全部已部署）：
1. 会话记忆隔离：每连接独立 agent（原全连接共享一份 `_memory`，并发互串上下文——**正确性缺陷已根治**）
2. RAG 全库一次检索（1 embed + 1 矩阵余弦 + 1 全库 BM25，替代逐条目 ×91 循环）+ 专用 2 线程池 + 查询 TTL 缓存 180s
3. 音频编码（mp3→WAV+base64）挪出事件循环
4. TTS 句级缓存（固定文本只合成一次）
5. uvicorn `--workers 3` 多进程（`run_server.py --workers 3`）
6. store_message 异步化 + EmbeddingCache/QuestionLog 修复

**根因修正**：凌晨报告"RAG 同步检索阻塞事件循环"不准确——检索本就走 to_thread；真实机制是 **96 路 GIL 密集检索任务（每查询循环 91 条目的 BM25/jieba/读盘）灌满共享默认线程池、事件循环被饿死**。另外两个并发雪上加霜项：音频编码直接跑在事件循环上；全连接共享同一个 agent 实例。

## 复测结果（3×workers，同一判稳标准）

| 并发 N | 连接 | 轮成功 | RAG命中 | 开口 med/p95 (s) | 整轮 med/p95 (s) | 判定 |
|---:|---:|---:|---:|---:|---:|---|
| 1（热） | 1/1 | 2/2 | 1 | 3.4 / 4.4 | 3.9 / 4.7 | PASS（预热 2.3s，与单人历史一致） |
| 96（原失效点） | **96/96** | **192/192** | 96 | 17.2 / 29.3 | 20.9 / 35.1 | **PASS**（凌晨版：70-74/96 握手失败） |
| 128 | 128/128 | 256/256 | 128 | 22.9 / 41.6 | 26.6 / 45.4 | PASS（开口超 20s 舒适线，可用档） |
| 160 | 160/160 | 320/320 | 160 | 15.8 / 40.9 | 19.2 / 43.1 | PASS |
| **200** | **200/200** | **400/400** | **200** | **19.6 / 30.2** | **22.2 / 34.4** | **PASS（达标）** |

服务器全程无感：MemAvailable 37-40G（workers 后基线约 -7G）、load1 峰值 7.7、swap 12-13%、IO PSI ≤0.18%。

## 结论

- **200 人并发（文本问答+RAG）达成**：连接 100% 成功、开口中位压线 20s 判稳线。
- 体验分档（突发全部同时提问的苛刻口径）：≤16 流畅（3-6s）；≤96 良好-可用（17-23s）；≤200 可用（中位 ~20s，p95 30-41s）；错峰提问体验好得多。
- 过程中发现并修复：multiprocessing spawn 双重初始化（`__mp_main__` + import string 各跑一次 create_app）导致一个 worker 槽位崩溃循环——`__name__` 门卫修复（`25a9f2a`）。**遗留观察项**：共用服务器上偶发新 spawn 子进程静默死亡（supervisor 自动补位，压测全程连接 100% 未受影响），成因未定位（无 dmesg 权限），疑似外部信号。

## 复测须知

- 服务器启动脚本已改：`exec uv run run_server.py --workers 3`（备份 `~/ai_school_bak_capacity_20260906.tar.gz`、`start_server.sh.bak-single`）
- 指标采样已适配多进程（父+子进程求和）
- 压测脚本单客户端 ConnectionClosed 只记失败，不再炸整级
- 回滚：`pkill -f 'run_server[.]py'` → 解包备份 tar → `start_server.sh.bak-single` 恢复启动脚本 → 标准姿势重启
