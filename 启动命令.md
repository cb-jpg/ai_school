# Open-LLM-VTuber 启动命令

## 进入项目目录

```bash
cd /home/dbh/live2D/Open-LLM-VTuber
```

## 后台启动服务

```bash
scripts/start.sh
```

启动后访问：

```text
http://127.0.0.1:12393
```

## 检查服务状态

```bash
scripts/healthcheck.sh
```

也可以直接检查健康接口：

```bash
curl http://127.0.0.1:12393/health
```

## 前台启动（用于查看实时日志）

```bash
set -a
source .env
set +a
export PATH="$PWD/.venv/bin:$PATH"
.venv/bin/python run_server.py
```

按 `Ctrl+C` 停止前台服务。

## 停止后台服务

```bash
scripts/stop.sh
```

## 重启服务

```bash
scripts/restart.sh
```

## 查看启动日志

```bash
tail -f logs/startup.log
```

服务使用当前 `.env` 配置，LLM 为 OpenAI-compatible 配置，ASR 为 Web Speech 优先、SenseVoice 回退。
