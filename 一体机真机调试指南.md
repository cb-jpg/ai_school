# 一体机真机调试指南（USB 连 55 寸数字屏）

- **日期**：2026-09-16
- **目的**：用 USB 线把 Windows 笔记本连上 55 寸一体机（KP-AIJH-550，RK3588 / Android 12 / 1080×1920 竖屏），远程看清楚 App 在机器上真实的运行状态，替代"拍照 + 猜"
- **分工**：电脑端已备好（adb 已装）；你只需要在**触摸屏上点几下**开调试模式 + 插线 + 点"允许"

---

## 一、电脑端（已完成 ✅）

- adb 已下载安装：`D:\srp_project\man\tools\platform-tools\adb.exe`（Google 官方 platform-tools 37.0.1）
- 下文所有 `adb` 命令都用完整路径运行，不用配 PATH

## 二、一体机端：开"开发者选项"（第一次用必读）

Android 12 标准流程，在触摸屏上直接点：

1. 进 **设置**（App 列表里找齿轮图标；如果桌面没有设置入口，看机器侧面/下部的检修盖，有的厂商把入口藏工程菜单）
2. 找到 **关于设备/关于平板电脑**（通常在"系统"里）
3. 找到 **版本号**（不是 Android 版本），**连续点 7 次**，会提示"您已处于开发者模式"
4. 返回设置 → **系统 → 开发者选项**（有的直接出现在设置主列表）
5. 打开 **USB 调试** 开关，弹窗选允许
6. （备选保险）同一页如有 **无线调试**，也打开——万一 USB 口不好使还能走 WiFi

> ⚠️ 定制 ROM 常见拦路虎：设置里点了没反应 / 要密码 / 找不到"版本号"。遇到就拍一张设置界面的照片发过来，别硬试。
> ⚠️ 调试完记得回来把 USB 调试**关掉**（展机摆在公共区域，别留门）。

## 三、插线连接

1. **线**：普通数据线不一定行——看机器检修口里是什么 USB 母口。常见的两种：
   - 机器上是 **USB-A 方口**（和电脑上一样的扁口）→ 需要 **USB-A 公对公线**（不常见，可能要买/找）
   - 机器上是 **Type-C** → 普通数据线即可
2. 一头插机器（注意插机器**主机板**的 USB 口，别插到触控/麦克风模块上），一头插笔记本
3. 机器屏幕会弹 **"允许 USB 调试吗？"**（显示电脑指纹）→ 勾选 **"一律允许使用这台计算机进行调试"** → **允许**
4. 电脑上验证（我来跑）：

```
D:\srp_project\man\tools\platform-tools\adb.exe devices
```

看到 `设备号  device` 就是连上了（显示 `unauthorized` = 屏上还没点允许）。

## 四、连上后第一批要拍板的事（按顺序）

| # | 问题 | 命令 |
|---|---|---|
| 1 | **机器上装的是哪个版本？**（v2.0 是 versionCode 10；朋友已确认装的就是 v2.0 debug 包，此条为 30 秒复核——照片里没看到绿色诊断浮层不是旧包，恰恰是 IS_KIOSK 没触发的旁证） | `adb shell dumpsys package com.shishi.ai \| grep -E "versionCode\|versionName"` |
| 2 | 装没装 APK？还是用系统浏览器开的网页？ | `adb shell pm list packages \| grep -i shishi`；`adb shell dumpsys activity top \| grep -i ACTIVITY` |
| 3 | 物理分辨率/密度到底多少 | `adb shell wm size`；`adb shell wm density` |
| 4 | 原生截图（替代拍照，无摩尔纹） | `adb exec-out screencap -p > shot.png` |
| 5 | App 原生日志（MainActivity 的 mixed/textZoom 记录 + WebView 的 JS 报错全在这） | `adb logcat -d -s AISchool chromium` |

这五条跑完，"组件/框框不是本来样子"的原因基本就能定位到：**IS_KIOSK 没触发**（头号嫌疑，9-16 晚本地 A/B 复现已逐条对上照片，见第七节）/ **真适配 bug**（判定生效但页面仍乱）/ **机器在用浏览器开网页而非 APK**。第五节 Console 一行代码即可拍板。

## 五、chrome://inspect（真机上的 DevTools，最强）

电脑 Chrome 打开 `chrome://inspect/#devices`，勾选 **Discover USB devices**，机器上打开 App 后下方会出现 `com.shishi.ai` 的 WebView，点 **inspect** → 就是熟悉的 DevTools（Console / Elements / 在页面里跑 JS）。

现场直接贴进 Console 的体检代码：

```js
JSON.stringify({
  touch: navigator.maxTouchPoints,          // =0 则 IS_KIOSK 不会触发（重点怀疑）
  inner: innerWidth + 'x' + innerHeight,    // ≈420x746 才是 v2.0 生效
  vv: visualViewport.width + 'x' + visualViewport.height,
  screen: screen.width + 'x' + screen.height, // 物理屏
  dpr: devicePixelRatio,                    // ≈2.57 才是 v2.0 生效
  meta: document.querySelector('meta[name=viewport]')?.content,
  appVh: getComputedStyle(document.documentElement).getPropertyValue('--app-vh'),
  chrome: navigator.userAgent.match(/Chrome\/(\d+)/)?.[0]
}, null, 1)
```

判读：`inner≈420 / dpr≈2.57 / appVh 有值` = v2.0 生效，问题在具体页面；`inner≈1080 / touch=0` = 适配没触发，先修判定。

> ⚠️ **能不能 inspect 取决于 APK 类型**：只有 debug 签名的包开了 WebView 调试；朋友打的若是 release 包，`chrome://inspect` 会看不到 WebView。真看不到也不亏——上面第四节的 adb 命令够拍板大部分事实；需要 DOM 级调试时，让朋友在 `MainActivity.java` 加一行 `WebView.setWebContentsDebuggingEnabled(true);` 重打包即可。

## 六、无线调试备选（USB 口摸不到 / 没合适的线）

Android 12 原生支持，全程触摸屏上点：

1. 开发者选项 → **无线调试** → 打开 → 点 **使用配对码配对**（屏幕会显示 6 位配对码 + IP:端口）
2. 电脑上：`adb pair 屏幕上的IP:端口 屏幕上的配对码`，然后 `adb connect 同页面的IP:端口`（注意配对端口和连接端口是两个不同的端口）
3. 之后所有 adb 命令照用，前提是笔记本和机器连**同一个 WiFi**

## 七、9-16 晚本地复现结论（去现场前必读）

在电脑上用浏览器模拟 1080×1920 做了 A/B 对照（截图在 `debug-0916/`）：

- **A（模拟 IS_KIOSK 判定失效）**：与真机照片逐条吻合——标签逐字竖排（照片里的"单字标签"）、专题卡 492px=45% 宽偏左（走了 `lg: calc(50%-24px)` 桌面断点）、右侧大片空白、正文偏小、无人物
- **B（模拟判定正常，v2.0 全套生效）**：完全健康——视口钉 420、卡片满宽、字号层级正常

**⇒ 机器上 IS_KIOSK = false，v2.0 整套适配没触发，退回桌面断点。**
**⇒ 现场第一件事**（chrome://inspect Console，30 秒）：

```js
[navigator.maxTouchPoints, innerWidth + 'x' + innerHeight, screen.width + 'x' + screen.height, matchMedia('(pointer: coarse)').matches]
```

判读：`touch=0` → 红外屏没报触摸（修法：判定加 Capacitor 原生/粗指针兜底）；`innerWidth 短边 <640` → ROM 密度异常（修法：判定阈值/依据调整）。两个都正常 → 另查（如机器实际在用浏览器开网页而非 APK，`dumpsys activity top` 看）。

修复预案（等现场数据确认后实施）：`device-profile.ts` 的 `isLargeTouchViewport` 加兜底——短边 ≥640 且（有触摸 **或** Capacitor 原生 App **或** `(pointer: coarse)`）都算 kiosk，桌面前端不受影响。

**✅ 兜底修复已完成并推分支**（`fix/kiosk-detection-fallback`，commit 6975fd9，2026-09-17）：竖屏大屏在 maxTouchPoints=0 时按 Capacitor 原生/粗指针识别为 kiosk。typecheck 通过，本地带新代码 kiosk/桌面两形态渲染无回归；「touch=0+原生」组合本地仿真不了（patchright 在触屏笔记本上强制伪造 touch≥1），最终确认靠现场。**明天动作序列**：① chrome://inspect 贴上面的判读代码（确认失效腿+留证据）→ ② 让朋友从该分支打 debug APK 装机（流程同 v2.0）→ ③ 看效果：左下角绿色诊断浮层出现 + 卡片满宽手机形态 = 判定已生效；若浮层出现但布局仍有问题，那才是真适配 bug（当场 Elements 里查）。

## 八、常见坑速查

| 现象 | 原因/处理 |
|---|---|
| `adb devices` 只有 `unauthorized` | 屏幕上没点允许弹窗；重新插一次看弹窗 |
| 完全搜不到设备 | 换线（有的线只能充电不能传数据）/ 换机器上的口 / 改走无线调试 |
| `dumpsys package` 查无此包 | 机器上根本没装 APK，是在浏览器里开网页——那断点/适配行为完全另一套，先装 v2.0 APK |
| logcat 刷屏 | 加 `-s` 只看指定 tag；`-d` 只看历史不停留 |
