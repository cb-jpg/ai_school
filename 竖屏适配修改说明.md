# 竖屏 55 寸数字屏适配修改说明

- **日期**：2026-09-15
- **分支**：`fix/portrait-board-55inch`（commit `477638c`，作者 wow-wogua）
- **目标设备**：55 寸竖屏数字屏，安卓 12，CSS 视口约 1272×2800，DPI≈40（~0.53mm/CSS px）
- **范围**：仅前端（`frontend/`），不动 Python 后端

## 一、症状（problems/ 两张截图）

1. 内容按横屏/桌面宽度渲染，整体放大、左侧被裁切（顶部导航行整行偏左溢出、引号文案被切断）
2. 下半屏大面积空白（只有虚化校园背景，无 UI、无人物）
3. 大数字「13」「100+」字号异常巨大，与正文比例失衡
4. 对话界面同样崩坏：680px 卡片左上吊挂、纵向拉满 75vh

## 二、根因（桌面模拟器 1272×2800 复现实证，非猜测）

| # | 根因 | 证据 |
|---|---|---|
| 1 | **Chakra 断点误命中**：App 只有两档设计（<768 手机竖屏 / ≥768 桌面横屏）。竖屏 1272px ≥768 命中 md/lg，走桌面横屏 hero 布局（左窄栏 520px + 右侧留白、固定 px 大字号 34/52px），内容堆顶部 | 复现截图与症状截图形态一致；lg 档实测字号 34px（标题）/52px（数字） |
| 2 | **横向溢出**：关闭态设置抽屉 `position:fixed` + `translateX(100%)`，视觉藏在屏外但矩形仍在 1272~1592px，撑出 body 横向可滚空间；App 的 body 滚动守卫只复位 `scrollTop` 不复位 `scrollLeft`，Android WebView 横向平移后不回位 | `bodyScrollWidth=1348 > 视口 1272`，溢出源矩形实测 |
| 3 | **Live2D 站位适配被整体跳过**：`use-live2d-model.ts` 的站位适配以 `innerWidth >= 768` 为门槛直接 return，竖屏大屏被误判为"桌面"，人物不做任何缩放站位（SDK 默认值） | 代码门槛 + 真机下半屏无人物 |
| 4 | **大屏低 DPI 无缩放策略**：~0.53mm/CSS px，52px 数字物理高约 27mm | 换算 |

## 三、修复方案（用户选定：方案 A「竖屏大屏专属排版」）

核心：新增第三种独立设备形态——**竖屏大屏**（`orientation: portrait` 且宽度 ≥768），与手机竖屏、桌面横屏互不干扰。

### 改动文件（9 个）

| 文件 | 改动 |
|---|---|
| `hooks/utils/use-portrait-board.ts` **（新增）** | 竖屏大屏检测 hook（matchMedia，含变化监听） |
| `index.css` | body/html 加 `overflow-x: hidden`：屏外抽屉等元素不再撑出横向可滚空间（配合守卫双保险） |
| `App.tsx` | ① body 滚动守卫补 `scrollLeft` 归零；② hero 路由 Live2D 画布层在竖屏大屏走手机同款全宽穿透（100% 宽、zIndex 15），而非 md 档"右 55%/低层级" |
| `hooks/canvas/use-live2d-model.ts` | ① 站位适配门槛由 `innerWidth>=768` 改为**方向判定**（横屏桌面才跳过，竖屏一律适配）；② 新增大屏站位常量 `HOME_BOARD_*`（首页：居中、下方展示区）/ `HERO_BOARD_*`（对话：右侧小比例、轻压卡片右缘） |
| `components/hero/home-page.tsx` | 首页：内容列限宽 840 居中；标题/大数字按视口 `clamp()` 缩放（标题 40~56px、数字 56~76/48~64px，768~2160 宽连续自适应）；正文/按钮/间距放大（按钮 84px 高远距触控）；纵向分布填满屏高，人物居中站下方展示区 |
| `components/hero/hero-landing.tsx` | 对话界面：对话卡限宽 840 居中、铺满高度（手机同款形态放大），桌面悬浮设置按钮在竖屏大屏隐藏（改由导航栏入口） |
| `components/hero/dialog-box.tsx` | 对话卡内部：消息 18px/气泡内边距放大、输入框 64px 高/20px 字、麦克风-发送键 56px、右侧为人物站位留 ~190px 缩进 |
| `components/hero/navbar.tsx` | 顶栏：字号 28px、左右留白 `max(24px, calc((100vw - 840px)/2))` 与内容列对齐；中部导航放大；设置齿轮在竖屏大屏显示并放大 |
| `components/hero/topic-tab-row.tsx` | 专题选项行：按钮 54px 高/19px 字（首页与专题页共用，一并受益）；竖屏大屏单行横滑 |

### 三端形态总览

| 设备 | 形态 | 字号（标题/数字13） |
|---|---|---|
| 手机竖屏 <768 | 原样（真机校准形态） | 19px / 44px |
| 桌面横屏 ≥768 | 原样（md/lg） | 34px / 52px |
| **竖屏大屏 portrait ≥768** | **新增：840 限宽居中 + clamp 缩放 + 人物专属站位** | ~48px / ~70px（随视口连续缩放） |

## 四、验证

- `typecheck:web` 零错误；`npm run build:web` 通过（14.96s）
- **1272×2800 复验**：内容列精确居中（left=216=列边距）、顶栏与列对齐、开始对话按钮居中 84px 高、`overflow-x hidden` 生效且**横向不可平移**（左切根因消除）；对话卡 840 居中铺满
- **无回归**：1920×1080 横屏（34/52px 原值）与 390×844 手机（19/44px 原值）布局字号与改前一致
- 复现脚本与前后对比截图（Playwright，1272×2800 模拟）已完成使命，本地已清理，未提交

## 五、交付（重打 APK 链路）

1. 确认合并 `fix/portrait-board-55inch` → `main`
2. `frontend/` 下 `npm run android:sync`（build:web 已验证通过，随后自动 cap sync）
3. Android Studio 打包签名 → 安装到数字屏

## 六、遗留事项

1. **站位常量待真机微调**：`HOME_BOARD_*` / `HERO_BOARD_*`（use-live2d-model.ts 顶部，带注释）是桌面模拟初值。真机上人物大小/位置不合适改这几个常量即可（与手机端四轮微调同套路）。
2. **屏上「点击重新连接」（本次未修，属配置问题）**：App 的 WebSocket 连 `127.0.0.1:12393`——后端不在屏内置系统上运行就连不上。需确认 APK 内服务器地址配置。⚠️ 后端不通时 Live2D 人物也不会加载：真机"下半屏无人"一半是布局（已修），一半是它。
3. 专题页（CampusKnowledge）仅吃到共用导航行的放大，整页大屏适配不在本轮范围。
